#include <stdio.h>
/**
 * main - Entry point to print single digit numbers
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int ch = 0;

	while (ch = 0)
	{
	putchar(ch);
	ch++;
	}
	putchar('\n');
	return (0);
}
